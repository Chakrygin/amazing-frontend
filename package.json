{
  "private": true,
  "type": "module",
  "scripts": {
    "build": "ncc build ./src/main.ts --minify",
    "clean": "if exist dist rd /s /q dist > nul"
  },
  "dependencies": {
    "@actions/core": "^1.10.1"
  },
  "devDependencies": {
    "@types/node": "^20.14.2",
    "@vercel/ncc": "^0.38.1",
    "tsx": "^4.15.1",
    "typescript": "^5.4.5"
  }
}
